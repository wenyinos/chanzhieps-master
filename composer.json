{
    "name": "chanzhi/chanzhieps",
    "description": "ChanzhiEPS - Enterprise Portal System built with PHP",
    "type": "application",
    "license": "ZPL-1.2",
    "homepage": "http://www.chanzhi.org",
    "require": {
        "php": ">=8.2",
        "ext-pdo": "*",
        "ext-mbstring": "*",
        "ext-json": "*",
        "ext-gd": "*"
    },
    "suggest": {
        "ext-iconv": "Required for character encoding conversion",
        "ext-curl": "Required for HTTP requests and OAuth",
        "ext-zip": "Required for theme and plugin import/export",
        "ext-xml": "Required for XML parsing and sitemap generation",
        "ext-session": "Required for user session management"
    },
    "config": {
        "vendor-dir": "vendor"
    }
}
